Prompt: Implementar Task #20 - Suporte Multi-Provider LLM (OpenAI + Groq) - Estruturando a implementação
Responsável: Welton Sabino
Usuário: welton-sabino
Data/hora: 2026-07-14 01:00:00

## Prompt original

Vamos implementar a task #20: Suporte Multi-Provider LLM (OpenAI + Groq) do arquivo tasks.md

Implementar suporte para múltiplos provedores de IA no LogAnalyzer AI.

### Objetivo
Permitir que o usuário escolha entre OpenAI (GPT-4 pago) ou Groq (grátis) como backend de análise de logs, sem alterar a lógica do agente.

### Requisitos Funcionais
1. **Suporte a dois provedores:**
   - OpenAI: GPT-4-turbo-preview (pago, mas mais preciso)
   - Groq: LLaMA 2 / Mixtral (grátis, rápido)

2. **Configuração via variável de ambiente:**
   - `LLM_PROVIDER=openai` ou `groq`
   - `OPENAI_API_KEY=...` (se OpenAI)
   - `GROQ_API_KEY=...` (se Groq)

3. **Configuração via CLI:**
   - `python -m src.loganalyzer.main arquivo.log --provider groq`
   - Valor padrão: openai (manter compatibilidade)

### Pontos de Alteração
1. requirements.txt - Adicionar: `langchain-groq>=0.0.1`
2. .env.example - Adicionar: `LLM_PROVIDER=openai` + `GROQ_API_KEY=gsk-...`
3. src/loganalyzer/analysis/llm_interpreter.py - Factory pattern
4. src/loganalyzer/main.py - Argumentar --provider CLI
5. tests/test_analysis.py - Testes com provider=groq
6. README.md - Documentar Groq setup

### Critérios de Aceição
- ✅ `python -m src.loganalyzer.main file.log` → usa OpenAI (padrão)
- ✅ `LLM_PROVIDER=groq python -m src.loganalyzer.main file.log` → usa Groq
- ✅ `--provider groq` via CLI sobrescreve .env
- ✅ Fallback heurístico funciona em ambos os casos
- ✅ 76+ testes passando
- ✅ Sem alterar lógica do agente

### Ordem de Implementação
1. Adicionar langchain-groq em requirements.txt
2. Refatorar initialize_llm() com factory pattern
3. Adicionar suporte CLI
4. Atualizar testes
5. Documentar em README

Importante:
- Registrar os prompts
- Não commitar nada
- Fazer apenas a alteração solicitada
