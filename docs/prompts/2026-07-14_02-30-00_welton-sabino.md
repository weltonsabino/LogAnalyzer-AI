Prompt: Implementar Task #20 - Suporte Multi-Provider LLM (OpenAI + Groq) - Execução prática
Responsável: Welton Sabino
Usuário: welton-sabino
Data/hora: 2026-07-14 02:30:00

## Prompt original

Executar a implementação completa da Task #20 conforme especificado no arquivo tasks.md.

Implementar suporte para múltiplos provedores de IA no LogAnalyzer AI, permitindo escolha entre OpenAI (pago) e Groq (grátis).

### Escopo da Implementação

1. **Refatorar llm_interpreter.py:**
   - Adicionar import: `from langchain_groq import ChatGroq`
   - Modificar `initialize_llm()` para aceitar parâmetro `provider="openai"`
   - Implementar factory pattern (se provider=="groq" → ChatGroq, senão ChatOpenAI)
   - Ler `LLM_PROVIDER` do .env (padrão: "openai")
   - Validar chaves apropriadas (GROQ_API_KEY ou OPENAI_API_KEY)

2. **Atualizar main.py:**
   - Adicionar argumento CLI `--provider {openai,groq}` (opcional)
   - Passar provider para `initialize_llm()`

3. **Atualizar .env.example:**
   - Adicionar `LLM_PROVIDER=openai`
   - Adicionar `GROQ_API_KEY=your_key_here`

4. **Atualizar tests/test_analysis.py:**
   - Adicionar testes com provider="groq"
   - Testar fallback com ambos provedores

5. **Atualizar README.md:**
   - Documentar setup de Groq
   - Instruções para obter chave

### Critérios de Aceição

- ✅ `python -m src.loganalyzer.main file.log` → usa OpenAI (padrão)
- ✅ `LLM_PROVIDER=groq python -m src.loganalyzer.main file.log` → usa Groq
- ✅ `--provider groq` via CLI sobrescreve .env
- ✅ Fallback heurístico funciona com ambos provedores
- ✅ 76+ testes passando
- ✅ Pylint ≥ 9.9/10
- ✅ Sem alterar lógica do agente

### Condições para Execução

- ✅ langchain-groq já adicionado em requirements.txt
- ✅ .env.example parcialmente atualizado (falta GROQ_API_KEY)
- ✅ Todos os 7 pontos de alteração identificados
- ⏳ Implementação em progresso

Importante:
- Não commitar
- Não fazer push
- Apenas implementação local
- Manter padrão PT/EN (comentários PT, variáveis EN)
